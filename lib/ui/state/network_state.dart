enum NetworkState {
  INITIAL,
  LOADING,
  LOADED,
  EMPTY,
  NOT_FOUND,
  SERVER_ERROR,
  NO_CONNECTION,
  UNKNOWN_ERROR,
  INVALID_CODE,
  USER_EXISTENCE,
  INVALID_CREDENTIALS,
  INVALID_TOKEN,
}
